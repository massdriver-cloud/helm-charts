{{- if and .Values.aws.securityGroup.enabled .Values.aws.securityGroup.id -}}
{{- $securityGroupId := .Values.aws.securityGroup.id -}}
apiVersion: vpcresources.k8s.aws/v1beta1
kind: SecurityGroupPolicy
metadata:
  name: {{ include "scheduled-job.fullname" . }}
  labels:
    {{- include "scheduled-job.labels" . | nindent 4 }}
spec:
  podSelector: 
    matchLabels:
      {{- include "scheduled-job.selectorLabels" . | nindent 6 }}
  securityGroups:
    groupIds:
      - {{ $securityGroupId }}
{{- end }}