{{- if and .Values.aws.securityGroup.enabled .Values.aws.securityGroup.id -}}
{{- $securityGroupId := .Values.aws.securityGroup.id -}}
apiVersion: vpcresources.k8s.aws/v1beta1
kind: SecurityGroupPolicy
metadata:
  name: {{ include "adhoc-job.fullname" . }}
  labels:
    {{- include "adhoc-job.labels" . | nindent 4 }}
spec:
  podSelector: 
    matchLabels:
      {{- include "adhoc-job.selectorLabels" . | nindent 6 }}
  securityGroups:
    groupIds:
      - {{ $securityGroupId }}
{{- end }}